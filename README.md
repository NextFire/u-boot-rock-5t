# u-boot-rock-5t

```sh
git submodule update --init --recursive
# In the devcontainer
./build_uboot.sh
# On the host
./build_rkdeveloptool.sh
# Put the board in maskrom mode, then
./write_uboot.sh
```
